import 'package:supabase_flutter/supabase_flutter.dart';

class AppDependencies {
  AppDependencies();

  factory AppDependencies.fromSupabaseClient(SupabaseClient supabaseClient) {
    return AppDependencies();
  }
}
